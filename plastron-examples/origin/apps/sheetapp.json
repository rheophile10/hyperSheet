{
  "formatVersion": 1,
  "segments": [
    {
      "name": "sheetapp",
      "cels": [
        {
          "key": "sheetapp.program",
          "celType": "ValueCel",
          "metadata": { "key": "sheetapp.program", "segment": "sheetapp", "name": "program", "description": "the worksheet program marker. sheetapp is the origin-application its documents (origin-user segments) render against; it renders a doc's cels as a worksheet window (sheetdoc) and owns the new/save/load/close lifecycle. role:application (no origin-rendered entry → clears the one-direction rule)." },
          "v": "worksheet"
        }
      ]
    }
  ],
  "manifests": [
    { "name": "sheetapp", "version": "1.0.0", "description": "the worksheet program — its documents are origin-user segments", "role": "application", "kind": "origin-application", "dependencies": [] }
  ]
}
