{
  "name": "sheetapp",
  "cels": [
    {
      "key": "sheetapp.addsheet",
      "celType": "LockedLambdaCel",
      "metadata": {
        "key": "sheetapp.addsheet",
        "segment": "sheetapp",
        "name": "sheetapp.addsheet",
        "kind": "native",
        "description": "(state, {ref, name?, rows?, cols?}) -> create a fresh SPARSE sheet segment and tab it into the workbook via window.addSheet. The only minted cel is <seg>.dims ({rows, cols}, default 12x7) - addresses render from dims and a cel is born only when a value/formula is committed. name defaults to the guarded sheetN; an existing name is a no-op (idempotent - the =sheet verb re-fires)."
      },
      "locked": true
    },
    {
      "key": "sheetapp.regrid",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "sheetapp.regrid",
        "segment": "sheetapp",
        "name": "regrid",
        "kind": "native",
        "description": "(state, seg) -> re-derive a sheet's content formula after a cel is born into (or removed from) the segment: swaps the sheetcells(...) ref list inside every workbook tab formula rendering seg. Called by origin.commit when a commit births a new grid cel."
      }
    },
    {
      "key": "sheetapp.applyrename",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "sheetapp.applyrename",
        "segment": "sheetapp",
        "name": "sheetapp.applyrename",
        "kind": "native",
        "description": "(state, _, event) -> commit the rename box (✓ button or Enter)."
      }
    },
    {
      "key": "sheetapp.tabmenu",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "sheetapp.tabmenu",
        "segment": "sheetapp",
        "name": "sheetapp.tabmenu",
        "kind": "native",
        "description": "(state, {ref,index}, event) -> right-click / double-click a worksheet tab to open the floating rename box."
      }
    },
    {
      "key": "sheetapp.renamesheet",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "sheetapp.renamesheet",
        "segment": "sheetapp",
        "name": "sheetapp.renamesheet",
        "kind": "native",
        "description": "(state, {ref,index,name}) -> rename a sheet by ALIAS (set the tab title + record <seg>.alias), NOT by re-keying cels. Formulas/cell keys are untouched."
      }
    },
    {
      "key": "sheetapp.program",
      "celType": "ValueCel",
      "metadata": {
        "key": "sheetapp.program",
        "segment": "sheetapp",
        "name": "program",
        "description": "the worksheet program marker. sheetapp is the origin-application its documents (origin-user segments stamped applications:[\"sheetapp\"]) render against; it owns new/open/save/load/close + the worksheet window render (sheetdoc). role:application is derived from the application/ loader group."
      },
      "v": "worksheet"
    },
    {
      "key": "sheetapp.open",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "sheetapp.open",
        "segment": "sheetapp",
        "name": "sheetapp.open",
        "kind": "native",
        "description": "(state) - the workbook 📂 button: open a picker window listing the OPFS segment store's DOCUMENTS (store.list entries carrying an `app` field — docs, not library/application segments). Click a row to open it.",
        "example": "=on(\"click\", \"sheetapp.open\")"
      }
    },
    {
      "key": "sheetapp.pick",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "sheetapp.pick",
        "segment": "sheetapp",
        "name": "sheetapp.pick",
        "kind": "native",
        "description": "(state, name) - picker row click: close the 📂 picker window and origin.opendoc the chosen stored document."
      }
    },
    {
      "key": "sheetapp.delete",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "sheetapp.delete",
        "segment": "sheetapp",
        "name": "sheetapp.delete",
        "kind": "native",
        "description": "(state, name) - the 📂 picker row's 🗑: confirm, then store.del every stored version of the document and refresh the picker. Baked demo docs reinstall on the next fresh boot; user docs are gone for good. Deleting an OPEN doc doesn't evict its live cels — and closing that window re-saves it."
      }
    },
    {
      "key": "sheetapp.save",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "sheetapp.save",
        "segment": "sheetapp",
        "name": "sheetapp.save",
        "kind": "native",
        "description": "(state, ref) - the workbook 💾 button: derive the document from the window ref (win.<doc>.state) and saveUserSpace it (dehydrate its private closure to the OPFS segment store). Reopen from OPFS with origin.opendoc."
      }
    },
    {
      "key": "sheetdoc",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "sheetdoc",
        "segment": "sheetapp",
        "name": "sheetdoc",
        "kind": "native",
        "description": "(state, seg, title?, offset?) - open a worksheet window over the cels of document segment `seg`, rendered via the sheets segment's sheetgrid (reactive: the content formula references each grid cel). The window (program) is win.<seg>; the data stays in segment <seg>. Idempotent (re-open un-hides + raises)."
      }
    },
    {
      "key": "origin.opendoc",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "origin.opendoc",
        "segment": "sheetapp",
        "name": "origin.opendoc",
        "kind": "native",
        "description": "(state, name) - open a sheetapp DOCUMENT: loadUserSpace the origin-user segment from the store (hydrates both its parent app + its own cels), then render it as worksheet window(s) (sheetdoc). The launcher path for a recorded document (doc:<name>)."
      }
    },
    {
      "key": "origin.newsheet",
      "celType": "LockedLambdaCel",
      "locked": true,
      "metadata": {
        "key": "origin.newsheet",
        "segment": "sheetapp",
        "name": "origin.newsheet",
        "kind": "native",
        "description": "(state) - the Sheet app: create a fresh blank worksheet DOCUMENT (a new origin-user segment of sheetapp), seed an empty grid, and render it. Save with origin.savedoc."
      }
    },
    {
      "key": "sheetapp.seal",
      "celType": "LockedLambdaCel",
      "metadata": {
        "key": "sheetapp.seal",
        "segment": "sheetapp",
        "name": "sheetapp.seal",
        "kind": "native",
        "description": "(ref) the workbook 🔒 button: envelope-encrypt this document to your identity (sheetkeys.sealsheet) and download <doc>.sealed. Opens 👤 Profile if your wallet is locked."
      },
      "locked": true
    },
    {
      "key": "sheetapp.openSealed",
      "celType": "LockedLambdaCel",
      "metadata": {
        "key": "sheetapp.openSealed",
        "segment": "sheetapp",
        "name": "sheetapp.openSealed",
        "kind": "native",
        "description": "() the workbook 🔓 button: pick a .sealed file, decrypt it with your identity (sheetkeys.opensheet), and open it as a workbook. Opens 👤 Profile if locked."
      },
      "locked": true
    },
    {
      "key": "sheetapp.golive",
      "celType": "LockedLambdaCel",
      "metadata": {
        "key": "sheetapp.golive",
        "segment": "sheetapp",
        "name": "sheetapp.golive",
        "kind": "native",
        "description": "(state, ref) -> state. The 🕸️ toolbar button: take this workbook LIVE — make the doc collaborative (writers=[me] if unset, so edits route through the CRDT pipeline), join a room derived from the doc name via the signaling relay (peerjoin), and register the sheetsync inbound routes. Unlock your identity first."
      },
      "locked": true
    },
    {
      "key": "sheetapp.grant",
      "celType": "LockedLambdaCel",
      "metadata": {
        "key": "sheetapp.grant",
        "segment": "sheetapp",
        "name": "sheetapp.grant",
        "kind": "native",
        "description": "(state, ref) -> state. The 🤝 toolbar button: grant the connected peer(s) write access — add each present peer (sheetsync.peers) to <doc>.writers and ECDH-wrap + send them the sheet data key + writers + current op-log (sheetsync.share). After this both peers' edits flow encrypted over WebRTC."
      },
      "locked": true
    }
  ]
}
