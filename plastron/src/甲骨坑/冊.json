[
  {
    "name": "kernel",
    "version": "1.0.0",
    "description": "Kernel-internal seeds (IO, lifecycle, segment registry, trap-as-value error schema). Always present. The kernel closure is {kernel} alone — no library segment is a kernel dependency.",
    "dependencies": []
  },
  {
    "name": "lambda-source",
    "version": "1.0.0",
    "description": "Opt-in schema for round-tripping multi-line lambda/formula source bodies as string[] in dehydrated JSON. Inflate-side join is always-on; this schema's sourceDehydrate splits back for readable .json output.",
    "dependencies": []
  },
  {
    "name": "js-common-schema",
    "version": "0.0.1",
    "description": "protocol implementations for the 12 base JavaScript types: one SchemaCel per type (JSON Schema + protocol fn refs) plus isChanged/hydrate/dehydrate lambdas, bound by key. The declarative catalog lives in 甲骨.json; this segment owns the runtime side.",
    "dependencies": []
  },
  {
    "name": "host",
    "version": "1.0.0",
    "description": "Host-capability cells: console.log/warn/error, now, random. Read by each kind segment's compiler layer (wat as wasm imports; py via pyodide globals binding). Apps replace _fn entries to gate or sandbox capabilities at install time.",
    "dependencies": []
  },
  {
    "name": "wasm-types",
    "version": "1.0.0",
    "description": "SchemaCels for the WIT primitive types (wasm:bool, wasm:i32, wasm:u32, wasm:i64, wasm:u64, wasm:f32, wasm:f64, wasm:char, wasm:string). Discriminator kind:'wasm' marks cels as living in a wasm domain — bridge cels and per-kind precompute layers dispatch on it.",
    "dependencies": []
  },
  {
    "name": "csp",
    "version": "1.0.0",
    "description": "Runtime CSP capability probes (eval, wasm) surfaced as locked cels.",
    "dependencies": []
  },
  {
    "name": "js-compiler",
    "version": "1.0.0",
    "description": "The 'js' compiler cel — JS source (last expression a callable) → callable Fn via QuickJS-emscripten (a JS-in-wasm sandbox). new Function removed (wasm-only-functions): no DOM, no eval, no host APIs unless granted via the host segment. Gated on csp.wasm-available; the ~1MB runtime lazy-loads on first compile (singlefile variant — embedded wasm, bundles inline).",
    "dependencies": [
      "csp"
    ]
  },
  {
    "name": "builtins",
    "version": "1.0.0",
    "description": "Arithmetic operators (+ - * /) as LockedLambdaCels. Flushable; formulas referencing them error cleanly when removed.",
    "dependencies": []
  },
  {
    "name": "wat-compiler",
    "version": "1.0.0",
    "description": "The 'wat' compiler cel — WebAssembly text-format source → callable Fn via wabt.js + WebAssembly.instantiate. Gated on csp.wasm-available. wabt.js is dynamic-imported only on first compile.",
    "dependencies": [
      "csp"
    ]
  },
  {
    "name": "wasm-bytes",
    "version": "1.0.0",
    "description": "The 'wasm' loader cel — precompiled .wasm bytes (base64 inline, or a 'file-store:<path>' reference) + a WIT descriptor → callable Fn via WebAssembly.instantiate. The sibling to wat/py/js that takes bytes instead of source; unlocks importing any language precompiled to wasm (Rust/C/Zig/…). Export chosen by metadata.wasmExport; imports default to { host } with a metadata.imports provider cel for WASI/env shims; composite outputSchema → WasmHandle. Gated on csp.wasm-available. Plus wasm-to-js / js-to-wasm bridges. See plastron/docs/4-current/07-wasm/11-wasm-bytes-kind.md.",
    "dependencies": [
      "csp",
      "host",
      "wasm-types",
      "file-store"
    ]
  },
  {
    "name": "py-compiler",
    "version": "1.0.0",
    "description": "The 'py' compiler cel — Python source → callable Fn via Pyodide. Plus py-to-js and js-to-py bridge cels using Pyodide's built-in conversion. Pyodide is dynamic-imported only on first compile; main-thread in v1.",
    "dependencies": [
      "csp"
    ]
  },
  {
    "name": "php-compiler",
    "version": "1.0.0",
    "description": "The 'php' compiler cel — PHP source (named functions; pick one via the := php.<fn>{…} selector) → callable Fn via php-wasm. Each compile defines its source in a fresh PHP namespace (recompile-safe); the runtime call is async (returns a Promise — fireCel awaits it). Dynamic-imported on first compile.",
    "dependencies": []
  },
  {
    "name": "formula-compiler",
    "version": "1.0.0",
    "description": "kind \"formula\": defn bodies that are parameterized plastron formulas (not JS). (params) => body, compiled via compileFormula, called positionally. Lets layout/note/glue verbs be defined from the formula surface, editable + wiki-visible.",
    "dependencies": []
  },
  {
    "name": "file-store",
    "version": "1.0.0",
    "description": "Phase A. Pathlib-shaped fs.* fns (read/write/list/stat/rename/mkdir/rmdir/delete/exists) over OPFS in browser and node:fs/promises in CLI. One backend chosen sync at module load; module-singleton dispatch (descriptor cels file-store.backend / .root are read-only in Phase A). Ships the file-binary SchemaCel with size / isChanged / mime protocols. No content addressing, no history, no schemas beyond file-binary yet — see plastron/docs/4-current/09-storage/01-file-store.md for the shipped surface, plastron/docs/1-design/3-accepted/09-storage/opfs-file-store.md for the accepted Phase A design, and plastron/docs/1-design/1-under-consideration/09-storage/xit-versioned-files.md for the phased xit vision.",
    "dependencies": []
  },
  {
    "name": "html-template-parser",
    "version": "1.0.0",
    "description": "View layer. The html-template (inline) and html-template-ref (live-editable) FormulaCel parsers turn HTML-shaped templates with {{…}} interpolation into a render-spec ({vnode, mount, listeners}). Ships the memoSafe vnode / render-spec / string-list schemas the output flows through. Interpolation bodies are the kernel's S-expression formula language.",
    "dependencies": []
  },
  {
    "name": "dom",
    "version": "1.0.0",
    "description": "Painter. The RAF-batched dom.paint ChannelCel (per-state painter coalesces N render-spec enqueues into one frame), the pure dom.diffVNodes (vnode → JSON Patch, keyed reconciliation), dom.applyPatch (DOM mutation, browser-gated, document-injectable), dom.applyListenerDelta (global-listener reconciliation), and the patch schema. Off-browser the patch is produced and observable but DOM mutation is skipped. Consumes the vnode/render-spec schemas from html-template-parser and the registry mechanisms from dom/events.ts.",
    "dependencies": [
      "html-template-parser"
    ]
  },
  {
    "name": "segment-store",
    "version": "1.0.0",
    "description": "Chunk B of segment-lifecycle. Persistent storage for dehydrated segments under plastron/segments/<name>/<version>/{manifest,segment}.json with a plastron/index.json lookup table. Five async store.* ops (put/get/list/delete/has) composed over file-store's fs.* — no new I/O primitives. Uncompressed JSON v1; index-last + tmp-rename atomicity; version strings are opaque filename components. See plastron/docs/4-current/09-storage/02-segment-store.md for the shipped surface and plastron/docs/1-design/2-in-evaluation/segment-opfs-layout.md for the design.",
    "dependencies": [
      "file-store"
    ]
  },
  {
    "name": "opfs-seeding",
    "version": "1.0.0",
    "description": "Chunk C of segment-lifecycle. seedStore(state) populates the segment-store under plastron/ from the in-memory boot segments (the kernel closure), idempotently — a name@version already in the index is skipped. Host-called after createInitialState (NOT auto-fired at boot, to keep transient/test States off disk); writes via segment-store.putRaw so it can seed role:kernel segments. See plastron/docs/4-current/01-boot/ for the shipped surface and plastron/docs/1-design/2-in-evaluation/opfs-seeding.md for the design (incl. the deferred auto-fire + boot-from-OPFS pieces).",
    "dependencies": [
      "segment-store"
    ]
  },
  {
    "name": "cli-segment-export",
    "version": "1.0.0",
    "description": "Chunk F of segment-lifecycle. CLI-only: exportToDir(state, dir, opts) copies the plastron/ store (a segment + its transitive deps, kernel-closure excluded by default) to a chosen disk directory mirroring the store layout; importFromDir(state, dir, opts) brings it back via segment-store.put. Cels install only when file-store.backend === 'node-fs' (empty in browser builds). v1: dir format only (tar.gz/zip deferred). See plastron/docs/4-current/09-storage/03-cli-segment-export.md for the shipped surface and plastron/docs/1-design/2-in-evaluation/cli-segment-export.md for the design.",
    "dependencies": [
      "segment-store"
    ]
  },
  {
    "name": "sheet",
    "version": "1.0.0",
    "description": "Spreadsheet machinery. The `infix` FormulaCel parser (Excel-style =A1*2; A1 refs resolve to sibling sheet.<addr> cell keys and auto-wire into inputMap; SUM/MIN/MAX/AVG/IF, ranges, comparisons, & concat) plus the action cels sheet.commit-cell / .cancel-edit / .move-selection / .start-edit. The per-sheet data layer (N×M grid + selection/editing/formula-bar/dims control cels) is generated by the buildSheet factory and hydrated by the host. See plastron/docs/3-test-design/05-runCycle/spreadsheet-segment.md.",
    "dependencies": []
  },
  {
    "name": "user-space-ops",
    "version": "1.0.0",
    "description": "Chunk D of segment-lifecycle. User-space lifecycle ops as locked native cels: newUserSpace / saveUserSpace / loadUserSpace / closeUserSpace, plus the shared hydrate-closure helper (BFS the segment-store from a root, topo-order the not-yet-loaded transitive closure, hydrate in one call; idempotent). save persists only the user-space's PRIVATE closure (role:user-space deps whose applications array subsets the owner's); load auto-starts the parent application via hydrate-closure; close flushes the private closure root-first without evicting shared libraries. v1 defers dirty-flag auto-tracking and runUntilSettled-before-save. See plastron/docs/4-current/02-hydration/ for the shipped surface and plastron/docs/1-design/3-accepted/02-hydration/session-segments.md for the design.",
    "dependencies": [
      "segment-store"
    ]
  },
  {
    "name": "origin-lifecycle",
    "version": "1.0.0",
    "description": "The single public lifecycle surface for origin documents (segment-lifecycle umbrella). Composes the shipped ops — does NOT re-implement persistence. One descriptor channel origin.io + a stateful drain: slot FormulaCels on an origin-application (.open/.edit/.new/.save/.load/.close) and an origin-user (.flush) evaluate to a pure {op,…} descriptor (built by the origin.* verbs, which carry metadata.emitsTo:'origin.io') and emit; the drain dispatches with state — new→newUserSpace, save→saveUserSpace, open→hydrate-closure, close→closeUserSpace, load→sha/compat gate then loadUserSpace, flush→optional save. origin.fire(slotKey) is the discrete trigger (the kernel flush hook is the equivalent for the .flush slot). edit is deferred (editor being overhauled).",
    "dependencies": [
      "user-space-ops",
      "segment-store"
    ]
  },
  {
    "name": "segment-archive",
    "version": "1.0.0",
    "description": "Tiered + whole-workspace segment export/import as a role-foldered .zip (the .甲 archive). Five native ops: export-all / export-application / export-library / export-user (→ Uint8Array) and import (Uint8Array, opts.onlyRoles?). Built over dehydrate/hydrate + a zero-dependency zip core (CompressionStream('deflate-raw') + CRC32 + central directory; runtime-agnostic across browser/Bun/Node). The kernel closure is always excluded — it ships in the bundle and resolves on import. Layout: applications/ libraries/ user/ folders of <name>@<version>/{manifest,segment}.json plus plastron.index.json. Host wires the sink/source (browser download/upload, node-fs). See plastron/docs/1-design/2-in-evaluation/segment-archive.md.",
    "dependencies": []
  },
  {
    "name": "sound",
    "version": "1.0.0",
    "description": "Web Audio capability segment. Cels sound.play-tone (oscillator), sound.play-pcm (Float32 samples), sound.stop-all, plus sound.master-gain and sound.context-state. Browser-only — off-browser every fn is a silent no-op so app code is portable. Lazy AudioContext creation on first play (browser autoplay policy). Module-scoped runtime state (the context + active source set) is shared across States — matches the host/file-store singleton pattern.",
    "dependencies": []
  },
  {
    "name": "music",
    "version": "1.0.0",
    "description": "MIDI-shaped vocabulary over the sound segment: notename->MIDI, note() specs, miditrack() player vnode, and the music.play effect handler that schedules notes on the Web Audio clock. (sound is soft-resolved at runtime, not a hard manifest dep, so a lazy sound stays forgettable.)",
    "dependencies": []
  },
  {
    "name": "defn",
    "version": "1.0.0",
    "description": "Named-function definition channel: binder formulas enqueue {name, source, kind} requests; the drain commits EditableLambdaCels with definedBy/origin ownership, rename retirement, and the orphan sweep (binders are authoritative for a name's existence).",
    "dependencies": [],
    "role": "library"
  },
  {
    "name": "genesis",
    "version": "1.0.0",
    "description": "Structure channel: generator formulas ({genesis:true, cels}) commit batches stamped generatedBy; regeneration diffs, seed-vs-content preservation, ownership sweep — generators are authoritative for their bloom's existence.",
    "dependencies": [],
    "role": "library"
  },
  {
    "name": "checkpoint",
    "version": "1.0.0",
    "description": "Snapshot ring + restore + delta: =checkpoint(name) formulas snapshot user-space via dehydrate; restore wipes and rehydrates (the wake path); delta feeds review gates.",
    "dependencies": [],
    "role": "library"
  },
  {
    "name": "plastron-canvas",
    "version": "1.0.0",
    "description": "Formula-authored 2d graphics: rect/text/line/circle ops and canvas(w,h,…ops) → a <canvas> VNODE carrying a data-ops draw-spec; the dom painter replays it onto the 2d context after each paint.",
    "dependencies": [],
    "role": "library"
  },
  {
    "name": "viz-core",
    "version": "0.0.1",
    "description": "Medium-agnostic encoding layer shared by charts, maps, and the 3d/gpu renderer. A SCALE is a pure data spec, not a closure: linscale/bandscale/colorscale return {t,…} objects; scaleapply/bandwidth/ticks/scaleinvert/colorapply/ordinalcolor consume them, plus extent/nice helpers. All pure fns over plain values — no DOM, no 2d context — so off-browser unit-testable and serialisable into a data-ops/scene spec. A chart axis, a map projection, and a sim unit-map are the same scale.",
    "dependencies": [],
    "role": "library"
  },
  {
    "name": "charts",
    "version": "0.0.1",
    "description": "Charting vocabulary over the plastron-canvas draw-spec: barchart/linechart/piechart(labels, values [, x y w h]) turn column ranges into op LISTS that canvas() concats — =canvas(420, 260, barchart(turtles!A2:A8, turtles!B2:B8)). Pure fns, no module state; the range edges in inputMap re-render a chart when its sheet data changes. Value→pixel axes and the categorical palette come from viz-core (linscale/scaleapply/ordinalcolor).",
    "dependencies": [
      "plastron-canvas",
      "viz-core"
    ],
    "role": "library"
  },
  {
    "name": "docgraph",
    "version": "0.0.1",
    "description": "The documentation graph rendered as a wiki window (unified-metadata-model). wiki.open assembles an article for any cel/segment/layer — summary + doc metadata, formula source, links to called functions and input cels (inputMap = the resolved AST edges), backlinks, a force-directed neighborhood graph, an editable note saved to metadata.note. The W titlebar button on every window opens its layer's article; =wiki(name) opens from a formula. Navigation is handler re-entry; the article is a snapshot built at open time.",
    "dependencies": [
      "dom",
      "window",
      "forcegraph"
    ]
  },
  {
    "name": "forcegraph",
    "version": "0.0.1",
    "description": "Force-directed graph simulations as a reusable dom capability. fg.set lays a spec ({nodes:[{key,label?,size?}], edges, pin?, onNode?}) out to OVERLAP-FREE (springs + repulsion + collision separation, pin held at center) then FREEZES — no ticker; the sim runs at set time and during drags. fgview(id, spec, pos, zoom) renders canvas edges under draggable chips in a 100%-width box; positions are cels, so referencing them in a formula makes drags re-render reactively. Drag relaxes neighbors live and settles to no-overlap on drop; wheel zooms; click re-dispatches spec.onNode unless the pointer travelled.",
    "dependencies": [
      "dom"
    ]
  },
  {
    "name": "scheduler",
    "version": "0.0.1",
    "description": "The learn/lock/lose adaptive frame governor (onecell §6) as a reusable cel group. clock.tick(state, cost) folds the measured frame cost into a learnable EWMA cost model (clock.cost_ewma) per clock.mode, then writes the next clock.interval (ms to wait) + the achievable clock.fps and returns the interval. Decouples desired / device-sustainable / scheduled rates via a tiny feedback controller; the host loop schedules the next frame with the returned interval. governStep is pure (off-browser unit-testable). Replaces hand-rolled setInterval/rAF clock loops.",
    "dependencies": [],
    "role": "library"
  },
  {
    "name": "buffer-schema",
    "version": "0.0.1",
    "description": "A SchemaCel ('buffers') that snapshots a buffer struct — a plain object whose values may be typed arrays (Float32/Float64/Int8/Uint8/Uint8Clamped/Int16/Uint16/Int32/Uint32 Array) alongside JSON-native scalars/arrays (dims, counters, RNG state). dehydrate encodes each typed array as {__ta, b64}; hydrate reconstructs the live typed array; isChanged is ref-eq. Bind via metadata.schema:'buffers' on any cel holding sim/mesh/gpu buffers — the default dehydrate is JSON-only. The snapshot half of the CPU-resident-SoA / dense-ECS interior; generalises js-common-schema's uint8array to every numeric dtype + the struct.",
    "dependencies": [],
    "role": "library"
  },
  {
    "name": "asset-loader",
    "version": "0.0.1",
    "description": "asset.fetch(state, name [, {base?, cache?, onProgress?}]) → Uint8Array. Fetch a named binary asset from base+name, cached in OPFS at /plastron/assets/<name> so it downloads once and reloads instantly. Generalises doom's inline fetch; the one mechanism behind every heavy lazy tier (doom WAD, and rapier/maplibre/occt/three to come). Caching is best-effort over file-store — a missing OPFS degrades to a plain fetch; cache:false skips it.",
    "dependencies": [
      "file-store"
    ],
    "role": "library"
  },
  {
    "name": "origin",
    "version": "1.0.0",
    "description": "The starting point: the origin cel (readme as value), the freespace view + entry gesture, load/cels effects. Boot contract: createInitialState + hydrate paints one visible cel.",
    "dependencies": [
      "genesis",
      "html-template-parser",
      "dom",
      "plastron-canvas",
      "charts",
      "checkpoint",
      "sqlite",
      "docgraph",
      "xlsx",
      "music",
      "formula-compiler",
      "file-explorer",
      "defn",
      "sheet",
      "sound",
      "builtins",
      "sheetapp",
      "keystore",
      "profile"
    ],
    "role": "application"
  },
  {
    "name": "sheetapp",
    "version": "1.0.0",
    "description": "the worksheet origin-application: owns the document lifecycle (opendoc/newsheet/sheetdoc + save-on-close wiring) and the sheetapp.program marker. Documents are origin-user segments stamped applications:[\"sheetapp\"]. Reaches origin.run + the user-space-ops lifecycle at runtime via resolveFn; origin depends on it (loaded with origin, parked otherwise).",
    "dependencies": [
      "sheets",
      "window",
      "user-space-ops",
      "segment-store",
      "opfs-seeding",
      "origin-lifecycle"
    ]
  },
  {
    "name": "net",
    "version": "0.0.1",
    "description": "network governance gate — wraps fetch, logs traffic (netlog), enforces the host allowlist (netallow); the =fetch verb",
    "dependencies": []
  },
  {
    "name": "seal",
    "version": "0.0.1",
    "description": "Layer-2 at-rest encryption of SEALED segments. seal.lock(password) derives a data key (PBKDF2-HMAC-SHA-256) and installs the kernel's state.sealCipher / state.sealDecipher hooks: a get-SEALED segment cel encrypts (AES-256-GCM) to a { __sealed: 'ivB64.cipherB64' } marker on dehydrate and decrypts back on hydrate; with no decipher (locked) the marker stays sealed and unreadable. seal.lock() with no arg clears the hooks. Synchronous zero-dep pure-JS AEAD + KDF (the kernel seal hooks run inline in the sync dehydrate/hydrate paths). The password / data key never become cel values and never enter an archive.",
    "dependencies": []
  },
  {
    "name": "crypto",
    "version": "0.0.1",
    "description": "foundational crypto via WebCrypto (zero-dep, Bun + browser): Ed25519 sign/verify, AES-256-GCM encrypt/decrypt, ECDH P-256 wrap/unwrap. The private key is NEVER a cel value — the graph holds a CryptoKeyHandle (id + public); the non-extractable CryptoKey lives in the keystore (IndexedDB in the browser, in-process in Bun) and resolves at the effect site. Tier 2 WebAuthn PRF scaffolded; Tier 3 seed-phrase recovery.",
    "dependencies": []
  },
  {
    "name": "session",
    "version": "0.0.1",
    "description": "service-agnostic session token store (the home the removed vault left open). Holds live auth tokens for any service (supabase-auth is the first tenant) in a module-scope Map and mints SecretHandles over them: session.put stores tokens + writes a non-secret session.<name>.status cel formulas react to; session.handle mints a SecretHandle whose resolver closes over the store and dehydrates to name-only; session.peek snapshots status; session.forget signs out. The token never becomes a cel value and never enters an archive — session-only by default (cross-reload persistence delegates bytes to seal/crypto in a later stage).",
    "dependencies": []
  },
  {
    "name": "keystore",
    "version": "0.0.1",
    "description": "the user identity WALLET (encrypted-collaborative-sheetapp Phase 1): a passcode-locked keyring of EXTRACTABLE keypairs (ecdh P-256 wrap + Ed25519 sign), sealed PBKDF2-HMAC-SHA-256 -> AES-256-GCM into a portable blob. create/unlock/lock/reshuffle(+history)/changePasscode/setName/export/import/seedPhrase. Dual-capability: persists the sealed blob to OPFS when available (served), else session-only with manual import/export (file://). Keyring + private keys live module-scope, never a cel; only non-secret status cels (keystore.status/.name/.identity/.ecdhpub) reach the graph.",
    "dependencies": [
      "file-store"
    ]
  },
  {
    "name": "profile",
    "version": "0.0.1",
    "description": "the identity-wallet UI (encrypted-collaborative-sheetapp Phase 1): a gen-2 profile WINDOW over the keystore library. profileui render verb + profile.* handlers (create/unlock/lock/reshuffle/changepass/setname/seed/import/importfile/export) bridge draft cels -> keystore.* -> a status message. profilewin() genesis opens it (app:profileapp launcher).",
    "dependencies": [
      "dom",
      "keystore",
      "window"
    ]
  },
  {
    "name": "supabase-auth",
    "version": "0.0.1",
    "description": "Supabase GoTrue auth provider on top of the session store. supabase.auth(state, op, project, payload): op=signin (password grant), op=otp/magiclink, op=refresh (rotate via stored refresh token), op=signout. Reads public config cels sb.<project>.url / sb.<project>.anonkey; on sign-in hands tokens to session.put (the JWT never becomes a cel value) and surfaces the non-secret sb.<project>.auth status cel. IO via gated fetch; no service_role key.",
    "dependencies": [
      "session"
    ]
  },
  {
    "name": "supabase",
    "version": "0.0.1",
    "description": "Supabase PostgREST data plane (cloud twin of the sqlite library). supabase.data(state, op, project, payload): select/insert/update/delete/rpc over REST, borrowing the user JWT from the session store as the bearer (anon when not signed in). Reads config cels sb.<project>.url / sb.<project>.anonkey. Writes bump sb.<project>.<table>.rev — reference it in a select formula to re-fire on change (the same cel realtime pushes bump). supabase.realtime subscribes a table to live postgres_changes over a Phoenix WebSocket, bumping the same rev cel on each push. Returns rows on success.",
    "dependencies": [
      "session"
    ]
  },
  {
    "name": "supabase-storage",
    "version": "0.0.1",
    "description": "Supabase Storage (buckets) — the cloud twin of the file-store/OPFS library. supabase.storage(state, op, project, payload): upload/download/list/remove objects in a bucket, createBucket/listBuckets, over gated fetch with the session JWT (RLS-scoped). Reads config cels sb.<project>.url / sb.<project>.anonkey. No service_role.",
    "dependencies": [
      "session"
    ]
  },
  {
    "name": "supabase-demo",
    "version": "0.0.1",
    "description": "Worked Supabase example (the sqlite-demo analog). sbdemo.install lays down config + a reactive sbdemo.<p>.panel FormulaCel (auth status + live todo rev-count + subscription state via dom). Dispatch handlers sbdemo.signin/add/watch drive supabase.auth/data/realtime; the panel re-renders reactively, including on realtime pushes that bump sb.<p>.todos.rev.",
    "dependencies": [
      "supabase-auth",
      "supabase",
      "supabase-storage"
    ]
  },
  {
    "name": "sqlite",
    "version": "0.0.1",
    "description": "in-browser SQLite via @sqlite.org/sqlite-wasm over the opfs-sahpool VFS, in a Worker (persistent, incremental writes, no COOP/COEP — Pages-safe). sqlite.command (open/sql/seed/schema): seed bulk-loads a JSON array into a table; schema introspects tables/columns/PK/FK; every write bumps a sqlite.<db>.rev cel so SELECT formulas re-fire. Browser-only (the sahpool VFS needs a Worker); the engine ships as sidecar files next to index.html.",
    "dependencies": []
  },
  {
    "name": "window",
    "version": "0.0.1",
    "description": "the primitive content-agnostic TABBABLE window frame — wframe (one window, drag/resize/min/fullscreen/close + a tab strip) + the window.* handler family over per-window state cels (win.<id>.state). A tab references a content cel of any kind, so a sheet tabs next to DOOM.",
    "dependencies": [
      "dom"
    ]
  },
  {
    "name": "winapps",
    "version": "0.0.1",
    "description": "the windowed-app framework — generic UI (toolbar/toolbtn) + app install (app.install/app.installed: an assets manifest {url,opfs,sha} → OPFS, idempotent). App-agnostic; sheet-specific UI lives in sheets, wasm specifics in winapps-wasm, the frame in window.",
    "dependencies": [
      "dom",
      "file-store"
    ]
  },
  {
    "name": "sheets",
    "version": "0.0.1",
    "description": "the native spreadsheet grid as reusable lambdas (sheetgrid + sheetcell) producing a CONTENT vnode a window tab hosts — the perf fast path, sheet-unique rendering only.",
    "dependencies": [
      "dom",
      "winapps"
    ]
  },
  {
    "name": "winapps-wasm",
    "version": "0.0.1",
    "description": "wasm-app specifics of the windowed-app stack — wasmapp builds a wasm <canvas> window on the tabbable frame + the graph<->engine bridge + active-gated keys (wasmcanvas + wasmwin.focus/blur/key live here). Pairs with winapps app.install + a harness provider.",
    "dependencies": [
      "dom",
      "window"
    ]
  },
  {
    "name": "file-explorer",
    "version": "0.0.1",
    "description": "the OPFS file UI as a library capability: the explorer render verb (=explorer / explorerwin window content) + its nav/open/openSheet/refresh/delete/rename dispatch handlers + the upload()/download() verbs and their handlers. Pure render + handler-side OPFS reads write explorer.cwd/.preview/.listing so the content formula re-fires (inputMap doctrine). Extracted from origin so windows' explorerwin() no longer reaches up into the application.",
    "dependencies": [
      "dom",
      "file-store",
      "sheet-io",
      "window"
    ]
  },
  {
    "name": "sheet-io",
    "version": "0.0.1",
    "description": "worksheet save/open + serialization (CSV / XLSX / .甲) as a library capability: saveSheet/openAsSheet TS exports + sheetio.save/sheetio.open verbs + the materialize-as-sheet engine. Extracted from sheet-host so file-explorer (and the windowing stack) import serialization without the gen-1 view loop. XLSX path delegates to the xlsx segment at runtime; .甲 uses segment-archive zip.",
    "dependencies": [
      "xlsx",
      "segment-archive"
    ]
  },
  {
    "name": "doom",
    "version": "0.0.1",
    "description": "=doom() — Doom in a wasm window; the WASI harness + lazy asset load (doom.wasm + WAD from plastron.ca, via asset-loader's OPFS-cached fetch)",
    "dependencies": [
      "winapps-wasm",
      "wasm-bytes",
      "asset-loader"
    ]
  },
  {
    "name": "peer",
    "version": "0.0.1",
    "description": "WebRTC peer transport — two browsers share a live sheet; remote is setValue-only into an allowlisted namespace, 函 quarantine, no secret exfil",
    "dependencies": []
  },
  {
    "name": "xlsx",
    "version": "0.0.1",
    "description": "read/write .xlsx into/out of cels (the Excel I/O on-ramp); minimal hand-rolled ZIP + DEFLATE via Compression/DecompressionStream",
    "dependencies": []
  },
  {
    "name": "io-keys",
    "version": "0.0.1",
    "description": "keyboard routing as cels — keys.event/pressed/capture/route; input is source-agnostic data",
    "dependencies": []
  },
  {
    "name": "io-touch",
    "version": "0.0.1",
    "description": "mobile input — a virtual-key overlay writing the same io-keys cels (second producer through keys.capture)",
    "dependencies": [
      "io-keys",
      "dom"
    ]
  }
]
